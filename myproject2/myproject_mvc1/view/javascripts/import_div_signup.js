function loadSignup(){
	
    // selecting "div" (you can also select the element by its id or class like in css ) 
   load_page="view/codes/signup.php";
   $("#login").load(load_page) // load in the file specified
   
}
